name: openmandi_dealer
description: OpenMandi — dealer app. Discover verified farmers, offer, order, pay.
publish_to: none
version: 0.1.0

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  openmandi_ui:
    path: ../../packages/openmandi_ui

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
